---
declared-services:
  visual-recognition-service:
    label: visual-teste-service
    plan: free
applications:
- name: visual-teste
  path: .
  command: npm start
  memory: 64M
  services:
  - visual-teste-service
  env:
    NODE_ENV: production
